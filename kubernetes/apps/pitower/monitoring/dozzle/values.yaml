controllers:
  dozzle:
    strategy: Recreate
    containers:
      app:
        image:
          repository: docker.io/amir20/dozzle
          tag: v10.7.5@sha256:0ecac18a3491962002e9d6b9f9a363155131a4213e723ab86fc76dfee6e3cd8e
        env:
          DOZZLE_MODE: k8s
          TZ: Europe/Zurich
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities: {drop: ["ALL"]}
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            memory: 256Mi
    serviceAccount:
      identifier: dozzle
defaultPodOptions:
  automountServiceAccountToken: true
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000
    fsGroupChangePolicy: OnRootMismatch

service:
  app:
    controller: dozzle
    ports:
      http:
        port: 8080

route:
  app:
    enabled: true
    hostnames:
      - dozzle.wibrow.dev
    parentRefs:
      - name: envoy-internal
        namespace: networking
        sectionName: https

serviceAccount:
  dozzle: {}
