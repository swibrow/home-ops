controllers:
  dozzle:
    strategy: Recreate
    containers:
      app:
        image:
          repository: docker.io/amir20/dozzle
          tag: v10.9.0@sha256:7f01a2504f89788b60ad0efddd94472fd66f9a225c708356cdb815d9d8abd184
        env:
          DOZZLE_MODE: k8s
          TZ: Europe/Zurich
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities: {drop: ["ALL"]}
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            memory: 256Mi
    serviceAccount:
      identifier: dozzle
defaultPodOptions:
  automountServiceAccountToken: true
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000
    fsGroupChangePolicy: OnRootMismatch

service:
  app:
    controller: dozzle
    ports:
      http:
        port: 8080

route:
  app:
    enabled: true
    hostnames:
      - dozzle.wibrow.dev
    parentRefs:
      - name: envoy-internal
        namespace: networking
        sectionName: https

serviceAccount:
  dozzle: {}
