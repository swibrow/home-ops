---
apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus-config
  namespace: monitoring
data:
  config.yaml: |
    storage:
      type: postgres
      path: "postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable"

    ui:
      title: Pitower Status
      header: Pitower Status

    endpoints:
      - name: ArgoCD
        group: Infrastructure
        url: https://argocd.wibrow.dev
        interval: 5m
        conditions:
          - "[STATUS] == 200"

      - name: Grafana
        group: Monitoring
        url: https://grafana.wibrow.dev
        interval: 5m
        conditions:
          - "[STATUS] == 200"

      - name: Miniflux
        group: Apps
        url: https://miniflux.wibrow.dev
        interval: 5m
        conditions:
          - "[STATUS] == 200"

      - name: Homepage
        group: Apps
        url: https://home.wibrow.dev
        interval: 5m
        conditions:
          - "[STATUS] == 200"
