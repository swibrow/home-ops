apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
resources:
  - cronjob-populate.yaml
configMapGenerator:
  - name: bin-cache-mise-config
    options:
      disableNameSuffixHash: true
    files:
      - mise.toml
      - mise.lock
