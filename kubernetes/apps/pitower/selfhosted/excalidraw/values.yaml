controllers:
  excalidraw:
    replicas: 1
    strategy: RollingUpdate
    containers:
      app:
        image:
          repository: docker.io/excalidraw/excalidraw
          tag: latest@sha256:f7ee194addd607bf831d2af0f0a34463dd4225e426cf35199ef0b12a803398e9
          pullPolicy: IfNotPresent
        # probes:
        #   liveness:
        #     enabled: true
        #   readiness:
        #     enabled: true
        # resources:
        #   requests:
        #     cpu: 5m
        #     memory: 10Mi
        # limits:
        #   memory: 512Mi
        resources:
          requests:
            cpu: 10m
            memory: 14Mi
service:
  app:
    controller: excalidraw
    ports:
      http:
        # The excalidraw nginx image serves on :80 and ignores the PORT env.
        port: 80

route:
  app:
    enabled: true
    hostnames:
      - draw.wibrow.dev
    parentRefs:
      - name: envoy-external
        namespace: networking
        sectionName: https

persistence:
  data:
    enabled: true
    type: emptyDir
