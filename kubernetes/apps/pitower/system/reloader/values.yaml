reloader:
  podMonitor:
    enabled: true

  reloadStrategy: annotations

  deployment:
    # krr: reloader-reloader/reloader-reloader
    resources:
      requests:
        cpu: 5m
        memory: 34Mi
      limits:
        memory: 256Mi
