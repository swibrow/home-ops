---
# yaml-language-server: $schema=https://raw.githubusercontent.com/SchemaStore/schemastore/master/src/schemas/json/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
components:
  - ../../../../components/volsync
  - ../../../../components/kopiur
configMapGenerator:
  - name: volsync-config
    options:
      disableNameSuffixHash: true
    literals:
      - APP_NAME=qbittorrent
      - CLAIM_NAME=qbittorrent-config
      - SECRET_NAME=qbittorrent-volsync
      - STORAGE_SIZE=1Gi
      - RESTIC_REPO=s3:https://s3.eu-central-2.amazonaws.com/pitower-volsync-backups/media/qbittorrent
  - name: kopiur-config
    options:
      disableNameSuffixHash: true
    literals:
      - APP_NAME=qbittorrent-kopiur
      - CLAIM_NAME=qbittorrent-config
patches:
  # qbittorrent runs as uid 2000; the kopiur mover must match to read it.
  - target:
      kind: SnapshotPolicy
      name: qbittorrent-kopiur
    patch: |
      - op: replace
        path: /spec/mover/securityContext/runAsUser
        value: 2000
      - op: replace
        path: /spec/mover/securityContext/runAsGroup
        value: 2000
helmCharts:
  - name: app-template
    repo: oci://ghcr.io/bjw-s-labs/helm
    version: 5.0.1
    releaseName: qbittorrent
    namespace: media
    valuesFile: values.yaml
