---
crds:
  install: true
  keep: true

manager:
  image:
    repository: ghcr.io/dragonflydb/operator
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 128Mi

serviceMonitor:
  enabled: true
