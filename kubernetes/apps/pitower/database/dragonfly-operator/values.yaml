---
crds:
  install: true
  keep: true

manager:
  image:
    repository: ghcr.io/dragonflydb/operator
  # krr: dragonfly-operator/manager
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 128Mi

serviceMonitor:
  enabled: true
