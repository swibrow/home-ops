---
# See miniflux.yaml for why the tenant secret is basic-auth, why it carries
# host/port/dbname, and why target.template.metadata is pinned.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shared-paperless
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: shared-paperless
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: paperless
        password: "{{ .password }}"
        host: shared-rw.database.svc.cluster.local
        port: "5432"
        dbname: paperless
  data:
    - secretKey: password
      remoteRef:
        key: /database/tenants/PAPERLESS_PASSWORD
---
apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: paperless
spec:
  cluster:
    name: shared
  name: paperless
  login: true
  databaseRoleReclaimPolicy: retain
  passwordSecret:
    name: shared-paperless
---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: shared-paperless
spec:
  cluster:
    name: shared
  name: paperless
  owner: paperless
  databaseReclaimPolicy: retain
