---
# See miniflux.yaml for why the tenant secret is basic-auth, why it carries
# host/port/dbname, and why target.template.metadata is pinned.
#
# The password is newly minted rather than carried over: the source credential
# was CNPG-generated on forgejo's own cluster, so there is nothing to preserve.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shared-forgejo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: shared-forgejo
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: forgejo
        password: "{{ .password }}"
        host: shared-rw.database.svc.cluster.local
        port: "5432"
        dbname: forgejo
  data:
    - secretKey: password
      remoteRef:
        key: /database/tenants/FORGEJO_PASSWORD
---
apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: forgejo
spec:
  cluster:
    name: shared
  name: forgejo
  login: true
  databaseRoleReclaimPolicy: retain
  passwordSecret:
    name: shared-forgejo
---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: shared-forgejo
spec:
  cluster:
    name: shared
  name: forgejo
  owner: forgejo
  databaseReclaimPolicy: retain
