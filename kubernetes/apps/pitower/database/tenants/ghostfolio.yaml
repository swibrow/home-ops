---
# See miniflux.yaml for why the tenant secret is basic-auth, why it carries
# host/port/dbname, and why target.template.metadata is pinned.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shared-ghostfolio
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: shared-ghostfolio
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: ghostfolio
        password: "{{ .password }}"
        host: shared-rw.database.svc.cluster.local
        port: "5432"
        dbname: ghostfolio
  data:
    - secretKey: password
      remoteRef:
        key: /database/tenants/GHOSTFOLIO_PASSWORD
---
apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: ghostfolio
spec:
  cluster:
    name: shared
  name: ghostfolio
  login: true
  databaseRoleReclaimPolicy: retain
  passwordSecret:
    name: shared-ghostfolio
---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: shared-ghostfolio
spec:
  cluster:
    name: shared
  name: ghostfolio
  owner: ghostfolio
  databaseReclaimPolicy: retain
