---
# proxmox role first: it fixes the apt repos (enterprise 401s otherwise) that
# fail2ban's package install depends on.
- name: proxmox-01 node (repo config + zfs + fail2ban)
  hosts: proxmox_01
  become: true
  roles:
    - proxmox
    - fail2ban
