# Talos cluster lifecycle, grouped under the `talos` module. Each submodule wraps
# one cluster's topf/talosctl recipes (shared via talos.justfile). Invoke as:
#   just talos pitower apply         # or  just talos::pitower::apply

mod pitower
